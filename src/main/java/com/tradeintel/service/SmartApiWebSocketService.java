package com.tradeintel.service;

import org.springframework.stereotype.Service;

/**
 * Obsolete. Logic moved to SmartApiService (SDK-Free Mode).
 */
@Service
public class SmartApiWebSocketService {
    // This class is no longer used to avoid JitPack SDK errors.
}
